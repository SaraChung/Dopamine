<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <%if user_signed_in?%>

  <nav class="top-bar">
    <section class="top-bar-section">
      <ul class="right">
        <li class="divider"></li>
        <li><%= link_to "Blog", blog_path(current_user.blog) %></li>
        <li class="divider"></li>
        <li><%= link_to "Profile", profile_path(current_user.profile) %></li>
        <li class="divider"></li>
        <li><%= link_to "Search Profiles", search_profiles_profiles_path %></li>
        <li class="divider"></li>
        <li><%= link_to "Matches", matches_users_path %></li>
        <li class="divider"></li>
        <li><a href="<%= messages_path %>">Messages</a></li>
        <li class="divider"></li>
        <li><%= link_to "Log Out", destroy_user_session_path, :method => :delete %></li>
      </ul>
    </div>
  </nav>

  <%end%>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
    <%= javascript_include_tag "application" %>
</html>